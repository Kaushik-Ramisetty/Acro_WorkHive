# Frontend environment for the unified HRMS app.
# Both Core (/auth/*) and Onboarding (/api/v1/auth/*) now run on the same FastAPI
# server. Point both VITE_API_URL (Core) and VITE_API_BASE_URL (Onboarding) to it.
VITE_API_URL=http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
VITE_USD_TO_INR=83
