// "Goals" surface for employees points at the unified Performance page.
// PMS Phase 1 collapses Goals + Performance into a single, role-aware hub
// so the same data model and state machine drives both entry points.
import PerformancePage from "./PerformancePage";

const GoalsPage = () => <PerformancePage />;

export default GoalsPage;
