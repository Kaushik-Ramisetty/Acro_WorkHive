// Deprecated. Replaced by ManagerDashboard.jsx ported from the ZIP reference.
// Kept as an empty stub because the project folder doesn't allow deletion.
export default function ManagerHome() {
  return null;
}
